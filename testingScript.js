get length variable

get what is selected

create variable that looks for each of the 4 uppercase,lowercase,
symbols, and numbers


if(upperCase.isClicked()){
    caseArray[0]===true;


}if(lowerCase.isClicked()){
caseArray[1]===true;

}if(symbol.isClicked()){
    caseArray[2]===true;
}if(number.isClicked()){
    caseArray[3]===true;
}
or
let caseArray=[];
caseArray.push(upperCase.isClicked)
caseArray.push(lowerCase.isClicked)
caseArray.push(symbolCase.isClicked)
caseArray.push(numberCase.isClicked)

for caseArray.length(){
    generatePassword();
}


var selectionNumber=


for(var i=0;i<)